# 图标替换结果

2026-09-20：继续已批准的“有更推荐的就更换”。接续时只有审查报告与候选预览，尚未修改图标引用；本轮完成以下替换。

| 功能 | 新图标 |
| --- | --- |
| 停止（共享引用） | 圆角方块 |
| 自动任务 | 书 |
| 自动战斗 | 卡牌 |
| 自动对话 | 消息气泡 |
| 挂接客户端 | 链节 |
| 最近导入 | 时钟 |
| 复制、复制日志 | 叠页 |
| 全部客户端范围 | 用户组 |
| 获取玩家 GID | 用户 |
| 暴击统计 | 击中 |

修改文件：src/gui/helpers.py、tab_hotkeys.py、tab_camera.py、tab_stats.py，以及 XuanShu.spec。原始 SVG 未改动；打包配置只添加使用到的 10 个资源。新增 tests/test_library_icons.py。

保留开始、导入／导出、传送阵、自由视角、支线任务和其他重置图标；不改按钮尺寸、回调和状态逻辑。停止图形单独缩小，避免实心方块过重。显式替换 currentColor，兼容现有主题切换。

修改前备份位于 before-replacement/。原有生产线改动未编辑或回滚。

验证：11 项相关界面测试、2 项资源测试通过；覆盖 10 个资源、两种颜色、14／16／24／32 像素渲染和模拟打包根路径。已查看 replaced-preview.png。git diff --check 通过。

边界：没有重新打包、覆盖现有 EXE 或进行真实桌面／游戏验证；打包资源配置和模拟路径检查不等同于新 EXE 实测。
