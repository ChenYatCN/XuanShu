# ibao-ST 集成

在 1.2 秒内连续点击顶部“脚本”三次，打开分组采集窗口。点击其他菜单会重置计数。

勾选已启动且已注入的客户端，设置角色翻页、切换间隔和地点，然后启动所选。
每次启动保存独立的配置副本；停止所选只停止勾选客户端。关闭配置窗口不会停止采集，退出玄枢会停止全部采集。

启动会返回角色选择界面，沿用原版角色轮换、宠物能力采集及补充快乐度逻辑。
角色需要位于地点配置中的区域，并具备原脚本要求的宠物能力与零食。
启动采集后，每个客户端独立监测采集结果、角色、地图及冷却变化；连续 180 秒无进展时自动重启对应账号并按原配置重新扫描采集。重复输入不刷新计时；冷却变化刷新计时；持续卡在加载界面仍会超时。连续三次重启后仍无收集会停止并提示原因，获得物品后重置连续次数。
自动登录要求客户端已关联玄枢账号库，建议从客户端管理启动账号。无法确定账号或重启失败时停止该客户端并记录错误。不会读取 accounts.txt。
其他采集客户端继续运行；停止所选会取消该客户端的采集和待执行恢复。已开始的底层登录可能仍完成，但不会继续采集。

来源：用户提供的 ibao-ST.py 和 Locations.txt（2026-09-11）。
保留原作者署名：Hailtothethrone、Nitsuj、Ultimate、Lxghtend、sydu8、ibao。
集成改动包括隔离运行状态、复用玄枢账号库恢复、停止时清理子任务、等待循环支持取消。

验证范围：分组、配置隔离、取消清理、三击入口、界面命令和语法检查。
未以实际游戏账号验证采集产出或角色轮换。

## 地点配置与统计

点击采集地点旁的铅笔图标，在表格中编辑名称、类型、坐标，可添加、删除、恢复默认、校验以及保存到所选账号。
只勾选一个客户端，点击“载入所选账号配置”恢复其已保存配置。启动时也会保存配置；要求客户端已关联账号。
计数兼容英文 Azoth 与中文“万灵秘药”获得提示。面板显示数量、运行时间、每小时产量、重启次数与停止原因。
统计与配置随玄枢设置保存，重启软件保留历史但不自动启动采集。“清空本轮统计”将当前记录归档，归零正在运行任务的计数与计时，保留历史。“导出记录”保存为 JSON。
